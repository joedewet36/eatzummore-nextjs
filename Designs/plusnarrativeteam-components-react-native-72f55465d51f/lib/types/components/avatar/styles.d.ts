import { Theme } from '@plus-components/types/theme';
declare const _default: (theme: Theme, size: 'small' | 'medium' | 'large') => {
    container: {
        borderWidth: number;
        width: number;
        height: number;
        overflow: "hidden";
        alignItems: "center";
        borderRadius: number;
        justifyContent: "center";
        backgroundColor: string;
        borderColor: string;
    };
    imageStyle: {
        width: number;
        height: number;
        alignSelf: "center";
        borderRadius: number;
    };
};
export default _default;
