import { Theme } from '@plus-components/types/theme';
declare const _default: (theme: Theme, variant: 'primary' | 'secondary' | 'outline' | 'danger', fullWidth: boolean, disabled: boolean, singleChild: boolean) => {
    container: {
        backgroundColor: string;
        borderColor: string;
        borderWidth: number;
        paddingVertical: number;
        flexDirection: "row";
        paddingHorizontal: number;
        borderRadius: number;
        width: string;
        justifyContent: "center" | "space-between";
    };
    label: {
        color: string;
    };
};
export default _default;
