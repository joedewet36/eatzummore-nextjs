import { Theme } from '@plus-components/types/theme';
declare const _default: (theme: Theme, active: boolean, size: 'default' | 'small') => {
    container: {
        borderWidth: number;
        borderRadius: number;
        paddingVertical: number;
        paddingHorizontal: number;
        backgroundColor: string;
        borderColor: string;
    };
    label: {
        color: string;
    };
};
export default _default;
