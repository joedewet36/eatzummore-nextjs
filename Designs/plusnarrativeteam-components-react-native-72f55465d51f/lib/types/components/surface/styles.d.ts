import { Theme } from '@plus-components/types/theme';
declare const _default: (theme: Theme, addBorders: boolean, fullWidth: boolean) => {
    container: {
        padding: number;
        borderWidth: number;
        width: string;
        backgroundColor: string;
        borderColor: string;
        borderRadius: number;
    };
};
export default _default;
